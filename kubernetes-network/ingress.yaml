apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nodework-ingress
  namespace: homework
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: homework.otus
      http:
        paths:
          - path: /homepage
            pathType: Prefix
            backend:
              service:
                name: nodework-service
                port:
                  number: 80
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nodework-service
                port:
                  number: 80
